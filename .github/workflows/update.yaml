name: 15-minute_check-new-post
on:
  schedule:
    - cron: '*/15 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl --request POST \
          --url 'https://cubablog.net/cron/check-new-post' \
          --header 'Authorization: Bearer ${{ secrets.API_SECRET_KEY }}'
